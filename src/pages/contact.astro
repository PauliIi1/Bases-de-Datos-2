---
import MainLayout from "../layouts/MainLayout.astro";
import siteConfig from "../site.config";
import { withBase } from "../utils/helpers";
---

<MainLayout activePage="contact" pageTitle="Presentación | Paula Buitrago">
  <div class="page-content text-center px-4">

    <h1 class="sigmar-ff text-balance">
      {siteConfig.contactInfo?.title}
    </h1>

    <img
      src={withBase("/foto.jpeg")}
      alt="Foto de Paula Buitrago"
      class="w-40 h-40 rounded-full mx-auto mt-6 mb-6 object-cover shadow-lg"
    />

    <p class="font-light text-balance max-w-2xl mx-auto mb-8">
      {siteConfig.contactInfo?.text}
    </p>

    <div class="flex justify-center my-10">
      <iframe
        width="800"
        height="450"
        src="https://www.youtube.com/embed/XvI8aJPXiYA"
        title="Video de presentación"
        frameborder="0"
        allowfullscreen
        class="rounded-2xl shadow-lg max-w-full"
      ></iframe>
    </div>

    <div class="mt-10">
      <p class="font-medium text-lg">
        {siteConfig.contactInfo?.email?.text}
      </p>

      <a
        href={siteConfig.contactInfo?.email?.href}
        class="text-blue-500 hover:underline text-lg"
      >
        {siteConfig.contactInfo?.email?.email}
      </a>
    </div>

  </div>
</MainLayout>
